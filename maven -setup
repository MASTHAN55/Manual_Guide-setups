1. create server.

2. install maeven git & tree.
   * yum install git java-1.8.0-openjdk maven tree -y
   * mkdir project1
   * cd project1

3. git clone (copy repo link) [pom.xml,src]

4. commands:
   * mvn compile
   * mvn test
   * mvn package
          (or)
4.1 Do all at a time (for compile,test,package at a time):
    * cd jenkins-java-project 
    * cd target
    * mvn clean package 
  
5. backup to maven homepath (.m2). 
   * mvn install
   * ll /root/.m2/repository/in/RAHAM/NETFLIX/1.2.2/

6. delete target folder:
   * mvn clean 

   
