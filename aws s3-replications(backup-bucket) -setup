                                                                                [Replication]
Note: Do what you need at particular time.
1. Bucket creation [Both buckets enable Bucket versioning]:
   1. source-bucket001
   2. dest-bucket002
  source bucket001 --> management --> Create Replication rules --> Replication rule name(rep-rule) --> Source bucket(Apply to all objects in the bucket) 
  --> Destination(Choose a bucket in this account) --> Bucket name(Browse s3) --> Destination storage class --> save.
   
