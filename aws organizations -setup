1. Organization Setup: [Do owner-account]
   Search aws organizations --> Create organization --> invite an existing aws account(tick) --> Email address or account ID of the AWS accounts to invite(ccit-0542845245) 
   --> Send invitation.
2. checking & accepting: [DO owner-account]
   search aws origanizations --> view 1 invetation[click] --> accept invetation. 

3. Create Service controle policies(scp): [Do owner account]
   search aws origanizations --> policies --> Service controle policies[click] --> Enable service controle policies --> Create policy --> policy name(restrict-leave)
   --> Add new statement --> leave origanization(Deny) --> Create policy.
4. Attach policy to users:
   --> actions --> attach policy(ccit-0542845245) tick --> attach policy.
   
   
   
